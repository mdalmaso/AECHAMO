function settings = read_file(obj,filename)

file = fopen(filename);

num = 0;

line = fgetl(file);
matches = [];

while (ischar(line) || isempty(matches) == 0)
    matches = strfind(line,'#')
    line = fgetl(file);
end

while ischar(line)
    matches = strfind(line, '#');
    if(isempty(matches) == 1)
        param_name = 

    end
    line = fgetl(file);

end